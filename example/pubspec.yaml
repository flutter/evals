name: example
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
publish_to: none

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  genkit: ^0.11.0
  genkit_google_genai: ^0.2.2
  genkit_mcp: ^0.1.2
  path: ^1.9.0
  framework:
    path: ../packages/framework
  devals_sandbox:
    path: ../packages/sandbox
  ai:
    path: ../packages/ai
  evals_results:
    path: ../packages/evals_results
  yaml: ^3.1.3
  schemantic: ^0.1.1

dev_dependencies:
  build_runner: ^2.13.1
  lints: ^6.0.0
  test: ^1.25.6
