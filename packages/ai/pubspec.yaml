name: ai
description: Framework-agnostic AI primitives and agent implementations for Dart.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
