name: evals_results
description: Output types and I/O for dart-evals (EvalResult, Score, EvalSetResult, etc.).
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  ai:
    path: ../ai
  equatable: ^2.0.7
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6


