name: framework
description: Core framework abstractions for evals.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  equatable: ^2.0.7
  logging: ^1.3.0
  devals_sandbox:
    path: ../sandbox
  genkit: ^0.11.0
  genkit_anthropic: 0.2.1
  genkit_middleware: ^0.2.1
  genkit_mcp: ^0.1.2
  path: ^1.9.1
  schemantic: ^0.1.1
  ai:
    path: ../ai
  evals_results:
    path: ../evals_results
  yaml: ^3.1.2
  genkit_google_genai: ^0.2.2
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
